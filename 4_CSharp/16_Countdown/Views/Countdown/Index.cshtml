<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Countdown</title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

</head>

<body class="container-fluid bg-dark bg-gradient text-light min-vh-100 py-3">
    <div class="container-fluid text-center">
        <div class="row justify-content-evenly">
            <div class="col-auto pt-3 px-4 border border-light rounded">
                <h3>Start Time</h3>
                @{
                <p>@DateTime.Now.ToString("MMM dd, yyyy")</p>
                <p>@DateTime.Now.ToString("hh:mm tt")</p>
                }
            </div>

            <div class="col-auto pt-3 px-4 border border-light rounded">
                <h3>End Time</h3>
                <p>Dec 25, 2022</p>
                <p>7:00 AM</p>
            </div>
        </div>

        <div class="row mt-3">
            <h1>Time Remaining</h1>
            @{
                DateTime EndTime = new DateTime(2022, 12, 25, 7, 0, 0);
                TimeSpan Remaining = EndTime - DateTime.Now;
                <h5>@Remaining.ToString("dd") Day(s), @Remaining.ToString("hh") Hours(s), @Remaining.ToString("mm") Minutes(s) Remaining</h5>
            }
        </div>
    </div>
</body>

</html>